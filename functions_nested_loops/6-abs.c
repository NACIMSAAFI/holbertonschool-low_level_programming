#include "main.h"
/**
 *int _abs - Function that prints the absolute value of an integer
 *@n: int value
 *Return: n or -n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
