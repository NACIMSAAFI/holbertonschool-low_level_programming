#include "main.h"
/**
 *print_numbers - Function that cmultiplies two integers.
 * Return: a*b
 */
void print_numbers(void)
{
	char ch = '0';

	for (ch = '0'; ch <= '9'; ch++)
		_putchar(ch);
	_putchar('\n');
}
